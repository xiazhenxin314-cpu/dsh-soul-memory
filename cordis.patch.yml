# dsh-soul-memory bundle patch layer:挂载插件行。
# 声明 dsh.bundle.patch 后,dsh plugin add 会把本包加入 profile 的
# dsh.profile.bundles,组合树自动应用本层,无需手动编辑 cordis.patch.yml。
- insert:
    - id: soul-memory
      name: '@xiaoxia/dsh-soul-memory'
