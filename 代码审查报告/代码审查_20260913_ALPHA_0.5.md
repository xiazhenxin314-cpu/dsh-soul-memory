# 代码审查报告 — ALPHA_0.5 纯 taste 整理(dsh-soul-memory)

- **审查对象**:工作树未提交改动 vs HEAD `2c7e9352fc7284209a71e4325d5b7b1e01638869`(2026-09-10,工作树基线与 doc/ALPHA_0.5 声明一致)
- **审查基准**:自称"零运行时行为变化"的大拆分(soul-memory.mjs 668→323 + 4 个新 lib 模块);以 `doc/ALPHA_0.5-代码库taste整理.md` 为被审文档,逐条对照实际 diff 与实机复跑
- **审查方式**:只读源码 + HEAD 基线在 /tmp 重建(`git archive`)双跑门禁 + 运行时导出面逐一比对 + 函数级 source diff;未修改任何源码,未执行 git commit
- **审查人**:独立代码审查员(ZCode) / 2026-09-13

## 门禁快照(审查员亲跑,node v22.23.1,与 doc 声明的版本一致)

| 步骤 | 命令 | 结果 |
|---|---|---|
| 工作树门禁 | `node tests/smoke.mjs` | **PASS: 59 assertions,EXIT=0** |
| HEAD 基线复跑 | `git archive HEAD` 重建至 /tmp(仅 symlink node_modules)后同款 | **PASS: 59 assertions,EXIT=0**(doc 的"改前 59"属实) |
| 语法检查 | `node --check` × 7(soul-memory.mjs、logfile、soul-service、messages、root-memory、logfile-write、memory-view) | 全部 OK |
| 导出面运行时比对 | `import()` HEAD 模块与工作树模块,`Object.keys` + 逐导出 typeof/String(fn) 比对 | **15 个导出名集合与顺序逐一相同**;仅 5 个函数源文本有差(见 §1),其余 9 项(name/inject/8 函数)逐字节相同 |
| re-export 绑定同一性 | `work.recallMemory === memory-view.recallMemory` 等 3 项抽查 | 全部 `true`(re-export 非包装,函数对象同一) |
| 全模块加载 | 9 个 .mjs 逐一 `import()` | 9/9 OK,无环、无未定义引用 |
| 行数 | `wc -l` | soul-memory 668→**323**;messages **29** / root-memory **131** / logfile-write **137** / memory-view **140**;logfile 323→**332**;soul-service 166→**165**;budget 78、migrate 54、tests 564 不变——**与 doc §4 数字全部吻合** |
| 挂载面 | `git status --short` / `git diff` | cordis.patch.yml、package.json 零 diff;patch 仅 `insert` id+包名,不引用任何文件路径;`main: soul-memory.mjs` 未动 |

---

## §1 零行为面核对表(大拆分重点)

### 1.1 导出面

- HEAD 定义序:name, inject, assertSectionKey, sectionBody, listSectionKeys, discoverMemoryLayers, renderMemoryBlock, recallMemory, appendRootMemory, correctRootMemory, appendLogfile, correctLogfile, createLogfile, cwdForProject, apply(共 15)。
- 新版 = name/inject 原样 + `export { … }` 块复述 11 个(顺序=原定义序)+ cwdForProject + apply。运行时 `Object.keys` 集合与顺序**逐一相同**。
- tests/smoke.mjs 深导入的自 soul-memory.mjs 的 11 个符号、自 lib/logfile.mjs 的 16 个符号全部仍可达(logfile 既有导出含 timestampHeading 一个未删)。
- 函数级 source diff(运行时取 `String(fn)` 后 diff)证实**只有 5 个导出函数文本变化**,且每处 hunk 均为下述已声明的等价变换;`sectionBody`/`listSectionKeys`/`assertSectionKey`/`discoverMemoryLayers`/`renderMemoryBlock`/`recallMemory`/`createLogfile`/`cwdForProject` 及 name/inject **逐字节未动**。

### 1.2 三个高风险迁移点(逐行比对结论)

| # | 点 | 结论 | 证据 |
|---|---|---|---|
| ① | BSN→`'\n'` | **值相等,成立** | HEAD `soul-memory.mjs:87` `const BSN = String.fromCharCode(10)` 确为 `'\n'`。全部 6 处使用(appendRootMemory :285、correctRootMemory :314 的 `BSN+BSN+'## '+…+BSN`;appendLogfile :333 拼接、:337 回执;correctLogfile :389 回执)均替换为 `'\n\n'`/`'\n'`,且全部处于左结合字符串 `+` 链中,无优先级/括号陷阱。拼接结果逐字符相同 |
| ② | readRawOrMissing 去重 | **语义等价,成立** | HEAD 两处(appendLogfile :323-328、correctLogfile :348-353)逐字相同:`const path = join(dir, nm+'.md')` + `try { raw = readFileSync(path,'utf8') } catch { throw new Error(logfileMissing(nm, dir)) }`。新 helper 用同一 `join(dir, nm+'.md')`、同一报错构造;执行次序未变(append:读原文先于 content 校验;correct:title 校验先于读)。唯一涟漪是写盘处 `writeTextAtomic(join(dir, nm+'.md'), …)` 重算一次 join——纯函数,零差异(见 P3 微瑕) |
| ③ | 迁移 memo Map 随迁 | **生命周期等价,成立** | HEAD:memo 为 soul-memory.mjs **模块级** `const migrationMemo = new Map()`(:164),唯一调用方 discoverMemoryLayers(:178),未导出;新版:同为 **lib/memory-view.mjs 模块级** Map(:38),唯一调用方 discoverMemoryLayers(:52)。ESM 每个解析 URL 单实例 → 进程级单例语义不变;smoke 与插件主体经 soul-memory.mjs re-export 与直接 import 到达**同一** memory-view 实例,"每项目根只试一次、只缓存成功"语义逐字未动 |

### 1.3 其余迁移保真抽查

- `lib/logfile.mjs` 仅 +existsSync import、+readTextSafe(函数体与 HEAD soul-memory.mjs:100-102 逐字同),既有导出零变化。
- `lib/soul-service.mjs` 仅删 `fail` 中转、`:160` 改 `throw new Error(created.error)`——同表达式,文件内已无任何 `fail` 残留。
- `apply()` 内去重:`readText`→lib `readTextSafe`(实现逐字同)、`writeText` 单点透传别名→`writeTextAtomic` 直呼(少一个无参透传,同参同调)、`scopeOf`/`homeOrProject`/`dirForScope` 三小助手替换的表达式与 HEAD :553/:593-594/:617/:626/:648-649 **逐字相同**。
- `插件说明.md`:diff 仅「开发与测试」模块清单行;**挂载四要素(类型/来源/目标 profile/安装命令)与验证/卸载段一字未动**;README/CHANGELOG 确实不涉及模块布局(grep 证实 README 无 `lib/` 引用),未动正确。
- doc/ALPHA_0.5 §1 各发现(F2-F6、F13 的行号与"逐字重复"断言)对照 HEAD 全部属实;F8(timestampHeading 全仓零引用)、F9(discoverMemoryLayers 的 rootPath 零消费)grep 复核属实;F10 引 CHANGELOG:58「lib/=ESM 源码入库」属实。

**§1 结论:零行为面核对全部通过,未发现任何声称与实物不符的实质性问题。**

---

## §2 发现与处置

无 F 级(阻断)发现。P 级 3 项:

| 编号 | 级别 | 位置 | 描述 | 处置建议 |
|---|---|---|---|---|
| P1 | P2(文档注释失实) | `soul-memory.mjs:27`(新文件头) | 模块布局注释称「lib/logfile.mjs / budget.mjs / migrate.mjs / soul-service.mjs **原样**」——但本批次恰好改了其中两个:logfile.mjs(+9,readTextSafe 迁入)、soul-service.mjs(fail 内联);与 doc/ALPHA_0.5 §3 自身的修改明细表自相矛盾 | 提交前顺手把该行改为「budget/migrate 原样;logfile(+readTextSafe)/soul-service(微调)见整理文档」之类的准确表述;纯注释,不影响门禁 |
| P2 | P3(观察,无行动) | `lib/logfile.mjs:256` | 深导入可及的 lib/logfile.mjs 公开面 **+1 导出**(readTextSafe)。纯增量,对既有消费方零风险;「15 导出面不变」红线本就限定在入口文件且已达成 | 登记备查即可;无需行动 |
| P3 | P3(微瑕,无行动) | `lib/logfile-write.mjs:54`、`:96` | appendLogfile/correctLogfile 写盘处重算 `join(dir, nm+'.md')`(HEAD 用单个 `path` 局部变量复用);同文件 createLogfile 仍保留 `path` 局部,风格略不对称。纯函数重算,零行为差 | 下次触该文件时可顺手还原局部变量;不构成批次门槛 |

doc 失实核查:除 P1(代码注释,非 doc 本身)外,doc/ALPHA_0.5 的基线数字、行数、发现清单、验证记录与实况**全部吻合**;「拆分中 EXIT=1(SyntaxError)后补 import 转绿」为历史中间态自述,无法复验但属诚实披露,不计失实。

---

## §3 工程 taste 评价

1. **四文件拆分边界:好。** 轴线选得准:messages(零依赖纯语料)← root-memory/logfile-write 两条写路径;logfile.mjs 保持文本层原语(命名/条目头/原子写/扫描);memory-view 收拢读侧(发现+冻结渲染+recall 路由);入口只剩插件壳。依赖分层无环(import 清单 + 9 模块加载实证):budget/migrate 仅被 memory-view 消费;唯一跨关注点边是 memory-view→root-memory(recall 的 root 小节读复用 sectionBody)——合理,读侧复用 L1 寻址本就该如此。
2. **soul-memory.mjs 323 行:内聚成立,不再拆正确。** 残余体量约七成是五件套 defineTool 的 description/parameters 文案(数据而非逻辑),再拆只会把声明式配置与接线分开,收益为负。
3. **messages.mjs 语料集中:无过度间接。** 29 行、零依赖、单跳命名导出;其存在有硬理由——`MSG.needContent/correctBan` 被两条写路径共享,不独立则 root-memory 与 logfile-write 必须横向互依。7 条 MSG 文案与 overLimit/illegalType/missingDescription 逐字未动。
4. **lib/=源码「不移动」判断:成立。** 事实链完整:零构建、CHANGELOG:58 明载、patch 只认包名(id `soul-memory` + `@xiaoxia/dsh-soul-memory`)、main 入口未动、lib 子文件本就是内部 import;新增内部模块不扩挂载引用路径集合。lib→src 改名是纯风险零收益,doc §5 的否决正确。
5. **新发现(taste 面即 §2 的 P1-P3):** 除文件头「原样」措辞失实外,未发现边界划分、命名、依赖方向上的新问题;`dirForScope` 的 home/else→project 二分与原内联语义逐字一致,不属新引入的隐式行为。

---

## §4 结论

**通过。**

- 零行为面(15 导出面、3 个高风险迁移点、挂载面、错误文案、渲染拼装、memo 单例)全部实证等价;门禁双跑(工作树 + HEAD 基线)均 59 assertions / exit 0。
- doc/ALPHA_0.5 与实物逐条吻合,是本仓目前质量最高的整理文档;仅新文件头一行注释措辞(P1)建议提交前顺手订正,P2/P3 登记不拦。
- 建议顺序:订正 P1 注释 → 同一工作树直接 `git commit`(门禁已绿,无需重跑;若订正后求稳可再跑一次 `node tests/smoke.mjs`,预期仍 59/exit 0)。

## 待决事项(留给功能批次,本批不动)

- `timestampHeading`(logfile.mjs:190,全仓零引用)是否删导出;
- `discoverMemoryLayers` 返回的 `rootPath` 死字段是否移除(对象形状变化);
- tests/smoke.mjs 564 行是否拆分(现门禁唯一入口,动它需单独立项)。
