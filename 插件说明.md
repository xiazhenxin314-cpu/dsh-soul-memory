# dsh-soul-memory 插件说明

## 如何挂载到 profile web

- **插件类型**:bundle 插件(dsh.bundle.patch + 包内 cordis.patch.yml),纯 ESM host 插件,无构建步骤
- **来源**:自研 @xiaoxia,MIT License;公开仓 [GitHub xiazhenxin314-cpu/dsh-soul-memory](https://github.com/xiazhenxin314-cpu/dsh-soul-memory)
- **目标 profile**:<DSH_HOME>/profiles/web

### 安装命令(每个部署独立执行一次)

```sh
git clone https://github.com/xiazhenxin314-cpu/dsh-soul-memory.git
DSH_HOME=<该部署的 home> dsh plugin --profile web add ./dsh-soul-memory
```

### 验证与生效

```sh
dsh --profile web --dump-config | grep -n -A2 -B2 "soul-memory"   # 该 id 恰好出现一次
```

然后重启 DSH 进程。DSH 本身是普通前台命令(`dsh web`),**不要求以系统服务方式运行**:
前台跑就 `Ctrl-C` 后重开,放 tmux/screen 就结束进程重开,只有 systemd 托管才用
`sudo systemctl restart dsh-<服务名>`。`link:` 挂载下改源码同样只需重启进程。

重启后 Tool.listTools 可见五件套:**memory_recall / memory_write_root / memory_write_logfile / memory_correct / memory_creator**;会话 system prompt 出现 SOUL 段(deployment:soul)与记忆总块(deployment:memory,`<memory>` 包裹)。

卸载:dsh plugin --profile web remove @xiaoxia/dsh-soul-memory,重启。
版本升级(仅改源码):改完源码重启即生效。

## 公开服务面 'soulMemory'(v0.3.0 新增)

工具面之外,本插件对外 provide 一个 cordis 公开服务 `soulMemory`,供其他插件(如 `dsh-top-tab`)消费——插件间协作走公开服务,不 value-import 对方实现:

- **readLayers(cwd?)** → 三层概貌结构化:`{ projectRoot, projectAvailable, root: { exists, text, sections[] }, home: Summary[], project: Summary[] }`,root 全文+小节化,home/project 为目录摘要(最近编辑在前,剥实现字段);
- **readLogfile(scope, name, cwd?)** → 单 logfile 结构化全文:`{ name, type, description, entries: [{title, stamp, body, lines}], preamble, truncated }`(128KiB 截断语义继承);
- **edit(payload, cwd?)** → 编辑,语义/错误文案/上限/.bak 与工具 pure 函数**严格一致**:`root.section.create / root.section / root.section.delete`(MEMORY.md 小节)、`logfile.entry.create / logfile.entry / logfile.entry.delete`(logfile 条目,at 时间戳消歧)、`logfile.shell`(建壳五参数);未知 target 报错透传。

实现零构建(mjs 纯函数封装,`lib/soul-service.mjs`),工具面/注入面完全不受影响;消费方(top-tab 类 UI 插件)各自维护同构契约类型。

## 功能摘要(v2.1,2026-08-27)

- **SOUL.md** -> 冻结注入最前段(order -10);
- **L1 MEMORY.md**(order 10)在 `<memory>` 大包裹内的 `<root>` 块完整展开;L2/L3 目录分别位于 `<home>`/`<project>` 块(name/type/description 忠实列出,块首行英文提示"全文用 memory_recall 取回");
- **五件套契约**:
  - recall:唯一读;
  - write_root:ROOT 小节纯追加(缺则新建);content 行首 #/## 禁止;超限拒绝给压缩指引;
  - write_logfile:日志纯追加,条目头 "## [标题] 时间戳" 全由工具生成,title 可选;
  - correct:跨层订正/删除——root 按 section、logfile 按条目标题命中,content 留空即删;必须唯一命中,多命中拒绝(logfile 用 at=时间戳消歧);
  - creator:建壳五参数(scope/name/type/description/heading),frontmatter 围栏+空行+# 一级标题由工具生成;无 body、无 overwrite(同名阻止创建,重建先人工删文件);
- **命名放开**:中文名可用;禁路径分隔/控制字符/Windows 保留字符/首尾点空格;≤64 字符 NFC;.md 自动剥;
- **预算 L1>L2>L3**:200 行或 16384 字符,L1 永不截断,低层从最旧截断并在所属层块末尾提示(memory_recall 可取回全量);
- **.bak 单代备份**:覆盖写前旧文件存 <path>.bak;
- v1 单文件 .memory 自动迁移(幂等+回滚)。

## 注入结构(v4)

运行时实际次序:deployment:soul(order -10,SOUL 原文)→ deployment:persona(order 0,**官方注入的身份行** "You are a coding agent powered by the {{model}} model…")→ deployment:memory(order 10,即总块)。总块形态:

```text
<memory>说明行(冻结语义 + 写前确认提醒)
---
<root>ROOT 介绍行
{MEMORY.md 快照全文}
</root>
---
<home>HOME 介绍行
{条目列表}
</home>
---
<project>PROJECT 介绍行
{条目列表}
</project>
---
</memory>
```

横线分隔均在尖括号之外;条目忠实展示不改写;截断/告警行放所属层块末尾。冻结语义 per-agent 不变:首次 assemble 渲染一次,会话字节稳定,新写入下个会话进目录。

## 文件约定(双工作者)

- MEMORY.md:纯 Markdown;`## key` 为模块键;追加 memory_write_root(section 必填)、整节重写/删除 memory_correct(scope=root);无前言约定;可手工编辑;
- logfile:creator 建壳 -> write_logfile 追加 -> correct 订正/删除;条目 = 二级时间戳小节;结构级改动(H1 更名/整体迁移/删文件)人工;.bak 兜底。

## 软审批(无硬闸)

写/订正/删前模型必须征得用户确认(规则在 <memory> 说明行与各工具 description);工具本身不设闸,policy=never 下可用。

## 安全与兼容性说明

- 读路径固定($DSH_HOME 四件 + 项目根 .memory);logfile 名禁含路径分隔与控制字符 => 无目录穿越面;
- L1 写入受 mem.config maxChars 上限约束;logfile 无独立上限(.bak + 预算兜底);
- 针对 @deepseek-ai/dsh 0.1.0-rc.6 开发;依赖官方 systemPrompt 段注册表内部形态。

## 已知限制

- complete:true persona 下所有记忆段不渲染(minimal/anchored-standard 实测受影响);
- 会话内写入不刷新冻结快照(设计如此);新建 logfile 下会话进目录;
- 并发写 last-write-wins;跨部署并行同理;
- 结构级操作(换一级标题/批量迁移/删整个文件)无工具通道,人工编辑文件系统。

## 开发与测试

- 纯 ESM 无构建;模块:soul-memory.mjs(入口/工具/v4 渲染)+ lib/logfile.mjs(命名/条目头/定位/.bak)+ lib/budget.mjs(预算)+ lib/migrate.mjs(v1 迁移)+ lib/soul-service.mjs(v0.3.0 公开服务面),纯函数全部导出供零挂载冒烟;
- npm test(node tests/smoke.mjs):59 断言(2026-09-04 v0.4.0 修复批次后全绿,含服务面契约段;计数同步两处:插件说明/README);
- 版本史:CHANGELOG.md;上手模板:templates/SOUL.md(部署 home 的 SOUL 起始模板)与 templates/memory-retain-prompt.md(任务收尾沉淀提示词)。
